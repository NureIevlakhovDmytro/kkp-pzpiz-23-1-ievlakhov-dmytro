# Point the app at the API. For Expo Go on a physical phone use your machine's LAN IP
# (find it on macOS with: ipconfig getifaddr en0). The phone and computer must share a network.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api
