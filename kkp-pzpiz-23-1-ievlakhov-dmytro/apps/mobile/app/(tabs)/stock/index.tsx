import { Text, View } from 'react-native';

export default function StockScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-muted-foreground">Залишки (M1)</Text>
    </View>
  );
}
